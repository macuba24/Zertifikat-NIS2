# Server-side (Vercel Function api/booking.js → Resend → Postfach)
RESEND_API_KEY=
BOOKING_TO_EMAIL=info@hampacorequality.de

# Optional: verified Resend sender (otherwise onboarding@resend.dev)
# BOOKING_FROM_EMAIL=HCQ Zertifikatsschulung <anmeldung@deine-domain.de>

# Optional: mailto fallback address (local Dev only)
VITE_CONTACT_EMAIL=info@hampacorequality.de

# Optional: always use mailto (local testing without API)
# VITE_FORCE_MAILTO=true
