# Server-side (Vercel Function api/booking.ts → Make.com)
BOOKING_WEBHOOK_URL=

# Optional: mailto fallback address
VITE_CONTACT_EMAIL=anmeldung@zertifikatsschulung.de

# Optional: POST directly to Make (CORS may block in browser)
# VITE_BOOKING_WEBHOOK_URL=

# Optional: always use mailto (local testing)
# VITE_FORCE_MAILTO=true
